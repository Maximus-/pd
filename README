**** This is out of date; Not currently reflective of the LLDB support.

PD -- Fork (not really) of PEDA for LLDB.

0. Credits
    - Original PEDA devs

1. Introduction
    PD is a Python LLDB script with many handy commands to help speed up 
    exploit development process on macOS/Unix. It is also a framework for 
    writing custom interactive Python LLDB commands.

2. Installation
    - Download
        $ git clone https://github.com/Maximus-/pd.git ~/.pd
    - Append a line to ~/.lldbinit to load PD when LLDB starts
        $ echo "command script import ~/.pd/pd.py" >> ~/.lldbinit
